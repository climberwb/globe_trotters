hello I am index
<% @conversations.each do |conversation|%>
  <%= conversation.team.name%> 
  <%=link_to conversation.team.name, messages_path(:messages => { :conversation_id => conversation.id }) %> 
<%end%>