<% "Chat" %>

<ul id="chat">
  <%= render @messages %>
</ul>
<%=  @team_chat %>
<%= form_for Message.new, :remote => true do |f| %>
  <%= f.text_field :body  %>
  <%= f.hidden_field :conversation_id, :value => @conversation_id%>
  <%= f.submit "Send" %>
<% end %>