<% "Chat" %>
  
<ul id="chat">
  <%= render @messages %>
</ul>

<%= form_for Message.new, :remote => true do |f| %>
<%= @team_chat%>
  <%= f.text_field :body  %>
  <%= f.hidden_field :conversation_id, :value => @team_chat %>
  <%= f.submit "Send" %>
<% end %>