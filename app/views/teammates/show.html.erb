<h1>Teammates#show</h1>
<p>Find me in app/views/teammates/show.html.erb</p>
<% if current_user.avatar? %>
        <div class="">
          <p>Current avatar</p>
          <%= image_tag( current_user.avatar.profile.url ) %>
        </div>
<% end %>
<%= @teammate.bio%>
<%= link_to  "click to find a team", teams_path %>