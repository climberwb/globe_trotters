class NotebookController < ApplicationController
  def edit
      # @notebook_sessions = current_user.notebook.icebreaker_sessions
      # if @notebook
      #   questions = []
      #   @notebook_sessions.each do |session|
      #     questions << session.question
      #   end
      #   icebreaker_session = IcebreakerSession.create(:question=>Question.first)
      #  @notebook = current_user.notebook.icebreaker_sessions  
  end

  def show
  end

  private


end

